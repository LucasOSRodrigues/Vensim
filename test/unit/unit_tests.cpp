#include <assert.h>
#include "unit_tests.h"

/**
 * @file unit_tests.cpp
 * @brief Unit tests for global functions.
 *
 * This file contains implementations of unit tests for global functions
 * (functions not belonging to any class).
 *
 * Currently, there are no global functions in the Vensim project,
 * so this file contains only the runner function stub.
 */

/**
 * @brief Runs all unit tests for global functions.
 */
void run_unit_test_globals(void) {
    // No global functions to test yet
}
